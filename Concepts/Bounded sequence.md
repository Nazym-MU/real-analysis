A sequence ($x_n$) is bounded if there exists a number $M > 0$ such that $|x_n| \leq M$ for all $n \in \mathbb N$.

This means that we can find an interval $[-M, M]$ that contains every term in the sequence ($x_n$).